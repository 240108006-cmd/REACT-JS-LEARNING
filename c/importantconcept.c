#include<stdio.h>
int main()
{
    int x = 2;
    if (x = 1)
    {
        printf("x is equal to 1 ");
        //it is giving output as x is equal to 1 because x =1 means all values except 0 is considered as true 
        // so the condition given in if statement is true so it is producing the output as x is equal to 1
    }
    else 
    {
        printf("x is not equal to 1 ");
    }



    return 0;
}