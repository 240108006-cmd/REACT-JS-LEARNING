#include<stdio.h>
int main()
{
    float a, b;
    printf("the value of a is ");
    scanf("%f",&a);// enter the value of a 
    printf("the value of b is ");
    scanf("%f",&b);// enter the value of b 
    printf("the sum of a and b is %f", a+b );
    return 0;
}